services:
  - type: web
    name: football-attendance
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.3
      - key: RATE_LIMIT_SECRET
        generateValue: true
